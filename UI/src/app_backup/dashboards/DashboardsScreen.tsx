import AppShell from '@/components/AppShell';
import { Box, Heading, Text } from '@chakra-ui/react'; 

export default function DashboardsScreen() {
  return (
    <AppShell>
      <Box p={6}>
        <Heading size="md">Dashboards (בבנייה)</Heading>
      </Box>
    </AppShell>
  );
}
